'use client'

export default function RootPage() {
    return (
        <main style={{ background: 'var(--bg)', minHeight: '100vh' }}>
            {/* Home page content deleted as per user request */}
        </main>
    )
}
